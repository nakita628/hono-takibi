openapi: 3.0.3
info:
  title: Minimal API
  version: 1.0.0

tags:
  - name: user
    description: User operations

paths:
  /users/{userId}:
    post: # ← method
      operationId: postUserRoute # ← routeName相当として運用しやすい
      x-routeName: postUserRoute # ← routeNameを明示したいなら拡張で
      tags: [user]
      summary: Create or update a user
      description: Minimal example including tags, method, summary, params, requestBody, responses.

      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string

      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string

      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                required: [id, name]
                properties:
                  id:
                    type: string
                  name:
                    type: string
